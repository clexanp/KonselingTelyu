import React from 'react';
import {StyleSheet, View, Text} from 'react-native';

const Menu = () => {
  return (
    <View>
      <Text>Menu</Text>
    </View>
  );
};

export default Menu;

const styles = StyleSheet.create({});
